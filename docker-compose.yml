services:
  database:
    image: postgres:18.1-alpine
    container_name: aerogo_db
    ports:
      - "5432:5432"
    env_file:
      - path: .env
        required: true
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
